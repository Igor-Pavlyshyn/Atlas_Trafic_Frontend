import CustomMap from "../../assets/home/Map.png";

const Map = () => {
  return (
    <section>
      <img src={CustomMap} alt="" width={994} height={274} />
    </section>
  );
};

export default Map;
