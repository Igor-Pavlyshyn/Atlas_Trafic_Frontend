import CustomMap from "../../assets/home/Map.png";

const Map = () => {
  return (
    <section>
      <img src={CustomMap} alt="map" width={"730px"} height={280} />
    </section>
  );
};

export default Map;
